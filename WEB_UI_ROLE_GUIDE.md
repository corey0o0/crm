# 웹 UI로 사용자 역할 관리하기 🎯

## 🚀 빠른 시작

### 1단계: 헬퍼 함수 설치

먼저 Supabase에 역할 관리를 위한 헬퍼 함수를 설치해야 합니다.

**Supabase Dashboard > SQL Editor**에서 다음 파일을 실행:
```
supabase/migrations/create_role_helper_functions.sql
```

이 파일은 다음 함수들을 생성합니다:
- ✅ `get_user_id_by_email` - 이메일로 사용자 ID 찾기
- ✅ `get_user_email` - 사용자 ID로 이메일 찾기  
- ✅ `assign_role_by_email` - 이메일로 직접 역할 할당
- ✅ `get_all_users_with_roles` - 모든 사용자와 역할 조회

---

## 📱 웹 UI 사용 방법

### 방법 1: 권한 설정 페이지에서 관리

1. **마스터 계정으로 로그인**
   - 이메일: `master@slimpack.com`
   - 비밀번호: `Cjfdls28gh!`

2. **권한 설정 메뉴 클릭**
   - 좌측 사이드바에서 "권한 설정" 찾기
   - (관리자만 볼 수 있음)

3. **사용자 관리 버튼 클릭**
   - 오른쪽 상단의 "사용자 관리" 버튼

4. **새 사용자에게 역할 할당**
   - "새 사용자에게 역할 할당" 버튼 클릭
   - 사용자 이메일 입력 (예: `kim@slimpack.com`)
   - 역할 선택 (관리자, 매니저, 일반직원)
   - "할당" 버튼 클릭

5. **역할 제거**
   - 사용자 목록에서 역할 Chip의 X 버튼 클릭
   - 확인 대화상자에서 "확인" 클릭

---

## 🎬 사용 예시

### 예시 1: 새 직원에게 일반직원 역할 부여

1. **권한 설정** → **사용자 관리** 클릭
2. **새 사용자에게 역할 할당** 클릭
3. 이메일 입력: `kim@slimpack.com`
4. 역할 선택: **일반직원**
5. **할당** 클릭
6. ✅ 성공 메시지 확인

**결과**: kim@slimpack.com은 대시보드, 고객 관리, A/S 관리, 게시판만 볼 수 있습니다.

---

### 예시 2: 매니저 승진

기존 일반직원을 매니저로 변경하는 방법:

1. **권한 설정** → **사용자 관리** 클릭
2. 사용자 목록에서 해당 직원 찾기
3. 기존 "일반직원" Chip의 X 클릭하여 제거
4. **새 사용자에게 역할 할당** 클릭
5. 이메일 입력: `lee@slimpack.com`
6. 역할 선택: **매니저**
7. **할당** 클릭

**결과**: lee@slimpack.com은 권한 설정을 제외한 모든 메뉴에 접근 가능합니다.

---

### 예시 3: 다중 역할 부여

한 사용자에게 여러 역할을 부여할 수 있습니다:

1. 첫 번째 역할 할당: **일반직원**
2. 두 번째 역할 할당: **매니저**

**결과**: 두 역할의 모든 권한이 합쳐집니다.

---

## 🎨 UI 화면 설명

### 권한 설정 메인 화면
```
┌─────────────────────────────────────┐
│  역할 및 권한 관리            [사용자 관리] [역할 추가]  │
├─────────────────────────────────────┤
│  ┌───────────┐  ┌───────────┐      │
│  │ 관리자     │  │ 매니저     │      │
│  │           │  │           │      │
│  │ [권한 설정] │  │ [권한 설정] │      │
│  └───────────┘  └───────────┘      │
└─────────────────────────────────────┘
```

### 사용자 관리 화면
```
┌─────────────────────────────────────────────┐
│  사용자 역할 관리      [새 사용자에게 역할 할당]  │
├─────────────────────────────────────────────┤
│  사용자            │  역할              │  │
│  ────────────────│──────────────────│  │
│  master@...       │  [관리자 ✕]        │  │
│  kim@...          │  [일반직원 ✕]      │  │
│  lee@...          │  [매니저 ✕]        │  │
└─────────────────────────────────────────────┘
```

### 역할 할당 화면
```
┌──────────────────────────────┐
│  사용자에게 역할 할당            │
├──────────────────────────────┤
│  이메일: [user@example.com  ] │
│  역할:   [▼ 선택하세요       ] │
│          - 관리자              │
│          - 매니저              │
│          - 일반직원            │
│                               │
│          [취소]  [할당]        │
└──────────────────────────────┘
```

---

## 🔍 역할별 접근 권한

| 메뉴 | 관리자 | 매니저 | 일반직원 |
|------|--------|--------|----------|
| 대시보드 | ✅ | ✅ | ✅ |
| 고객 관리 | ✅ | ✅ | ✅ |
| A/S 관리 | ✅ | ✅ | ✅ |
| 출고 관리 | ✅ | ✅ | ❌ |
| 파츠 관리 | ✅ | ✅ | ❌ |
| 매장 재고 관리 | ✅ | ✅ | ❌ |
| 입출고 관리 | ✅ | ✅ | ❌ |
| 매출 통계 | ✅ | ✅ | ❌ |
| 게시판 | ✅ | ✅ | ✅ |
| **권한 설정** | ✅ | ❌ | ❌ |

---

## ⚠️ 주의사항

### 1. 역할 변경 시 재로그인 필요
- 사용자의 역할을 변경한 후, **해당 사용자는 로그아웃 후 다시 로그인**해야 새 권한이 적용됩니다.

### 2. 관리자 계정 관리
- **최소 1명의 관리자는 항상 유지**해야 합니다.
- 마지막 관리자의 역할을 제거하지 마세요!

### 3. 이메일 정확성
- 역할을 할당할 때 **이메일을 정확히 입력**해야 합니다.
- Supabase에 등록된 사용자만 역할 할당이 가능합니다.

### 4. 권한 없는 페이지 접근 시
- 권한 없는 페이지에 접근하면 **403 에러 페이지**가 표시됩니다.
- "대시보드로 돌아가기" 버튼으로 복귀 가능합니다.

---

## 💡 팁

### 빠른 역할 할당
SQL Editor에서 직접 실행하면 더 빠릅니다:
```sql
SELECT assign_role_by_email('user@example.com', '매니저');
```

### 모든 사용자 확인
```sql
SELECT * FROM get_all_users_with_roles();
```

### 특정 사용자의 모든 역할 제거
```sql
DELETE FROM user_roles WHERE user_id = (
  SELECT id FROM auth.users WHERE email = 'user@example.com'
);
```

---

## 🆘 문제 해결

### ❌ "사용자를 찾을 수 없습니다" 오류
**원인**: 입력한 이메일이 Supabase에 등록되지 않음
**해결**:
1. Supabase Dashboard > Authentication > Users에서 사용자 확인
2. 사용자가 없으면 먼저 계정 생성
3. 정확한 이메일 주소 확인

---

### ❌ "역할 할당에 실패했습니다" 오류
**원인**: RPC 함수가 설치되지 않음
**해결**:
1. `create_role_helper_functions.sql` 파일 실행 확인
2. SQL Editor에서 다음 명령으로 확인:
```sql
SELECT routine_name FROM information_schema.routines 
WHERE routine_name = 'assign_role_by_email';
```

---

### ❌ 사용자 목록이 비어있음
**원인**: 아직 아무에게도 역할이 할당되지 않음
**해결**:
1. "새 사용자에게 역할 할당" 버튼 클릭
2. 첫 번째 사용자 추가

---

### ❌ 권한이 적용되지 않음
**원인**: 캐시된 권한 정보 사용 중
**해결**:
1. 로그아웃
2. 다시 로그인
3. 브라우저 캐시 삭제 (선택사항)

---

## 🎓 고급 사용법

### 커스텀 역할 생성

1. **권한 설정** 메인 화면에서 **역할 추가** 클릭
2. 역할 정보 입력:
   - 이름: `재고 담당자`
   - 설명: `재고 관련 메뉴만 접근`
3. **권한 설정** 버튼 클릭
4. 필요한 메뉴 체크박스 선택:
   - ✅ 대시보드
   - ✅ 매장 재고 관리
   - ✅ 입출고 관리
   - ✅ 파츠 관리
5. **저장** 클릭

이제 "재고 담당자" 역할을 사용자에게 할당할 수 있습니다!

---

## 📞 지원

문제가 계속되면 다음 정보와 함께 문의하세요:
- 발생한 오류 메시지
- 수행한 단계
- 브라우저 콘솔 로그 (F12 → Console 탭)

---

## ✅ 체크리스트

설정을 완료했는지 확인하세요:

- [ ] `create_role_helper_functions.sql` 실행 완료
- [ ] 마스터 계정으로 로그인 성공
- [ ] "권한 설정" 메뉴가 보임
- [ ] 첫 번째 사용자에게 역할 할당 성공
- [ ] 해당 사용자가 재로그인하여 권한 확인

모두 완료했다면 웹 UI를 통한 역할 관리 시스템이 준비되었습니다! 🎉

